import * as THREE from 'three';


const config = {
  ambientColor : '#d67a0c',
  velocityFactor : 25,
  crossFactor : 26,
  ballSize : 0.2,
  ballInitialPosition : new THREE.Vector3( 0, 2, 12 )
};

export default config;
//